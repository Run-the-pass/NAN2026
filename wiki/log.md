# 작업 로그

## [2026-07-24] ingest | NAN 2026 프로젝트 브리프

- `raw/project-brief.md`에 사용자 원문을 보관했다.
- `wiki/project.md`에 핵심 경험, 최소 시연 범위, 명령 책임 경계, 제작 순서를 정리했다.
- `AGENTS.md`에 LLM Wiki와 계획·작업·검증 운영 규칙을 정의했다.

## [2026-07-24] implementation | SLIME SHIFT 시연 버전

- 참고 이미지의 중앙 작업 공간과 외곽 스테이션 배치를 색상 사각형으로 구현했다.
- 1라운드 단일 명령, 배고픔 사고, 재지시와 2라운드 `PREPARE` 추상 명령을 구현했다.
- Gemini 오디오 명령 API, allowlist 검증, 결정론 코어 테스트를 추가했다.
- 검증 에이전트가 2라운드 전체 흐름, 테스트 3/3, 빌드, 린트와 잘못된 API 요청 처리를 `PASS`로 판정했다.

## [2026-07-24] implementation | 주문·작업 큐와 성장 선택

- 라운드 시작 주문과 10초 주기 추가 주문을 결정론적 코어 상태로 구현했다.
- 슬라임별 FIFO 작업 큐, 속도 기반 이동, 작업 시간, `IDLE/MOVING/WORKING` 상태를 추가했다.
- 라운드 사이 화면 중앙에 실제 효과가 다른 성장 카드 3장을 표시한다.
- HUD에 대기 주문, 다음 주문, 판매 수, 현재 작업과 큐를 표시한다.
- 테스트 9/9, 빌드와 린트가 통과했으며 브라우저에서 주문 주기, 이동, 큐, `IDLE`, 세 선택 흐름을 확인했다.

## [2026-07-24] ingest | 성장 선택 카드 참고 이미지

- 사용자 제공 이미지를 `raw/references/upgrade-choice-cards.png`에 원본 그대로 보관했다.
- 중앙 오버레이에 동일 크기 카드 3장을 배치하는 현재 선택 화면의 시각 참고 자료로 연결했다.

## [2026-07-24] implementation | 타일 주방·경로 이동·CLI

- 주방 전체를 16×10, 칸당 60px인 공용 타일 데이터로 통일했다.
- 슬라임이 고정 순서 BFS 경로로 벽과 작업대를 우회하고 이동 속도에 따라 연속 이동하도록 구현했다.
- 플레이어도 같은 타일의 통행 가능 여부에 따라 벽과 작업대를 통과하지 못한다.
- 렌더링 없이 seed와 명령을 받아 경과 시간, 작업 결과, 점수, 최종 상태를 JSON으로 출력하는 CLI를 추가했다.
- 독립 검증 에이전트가 완료 조건 13개를 `PASS`로 판정했다. 테스트 12/12, CLI 결정론·오류 종료, 린트, 빌드, 실제 브라우저 이동·충돌·카드·콘솔을 확인했다.

## [2026-07-24] implementation | 음식 위치와 서빙 인계

- 음식 위치를 버섯 상자, 패스, 말랑, 빨강 중 하나로 추적하고 Phaser 아이콘과 HUD 소지 정보로 표시한다.
- `SERVE`는 빨강이가 패스에서 스튜를 픽업한 뒤 고객에게 이동하고, 서빙 작업까지 끝나야 판매를 적용한다.
- 독립 검증 에이전트가 패스 픽업, 운반, 900ms 완료 경계와 판매 결과를 `PASS`로 판정했다. 테스트 13/13, CLI 결정론, 린트, 빌드, 브라우저 표시와 콘솔을 확인했다.

## [2026-07-24] ingest | 슬라임 스탯과 3분 펀테스트 전환

- `raw/slime-stats-pricing-and-penalties.md`의 타일 행동 규칙과 작업·이동·청력·집중력 레벨 표를 장기 밸런스 기준으로 정리했다.
- `raw/fun-test-design-changes.md`의 최신 결정을 현재 방향으로 반영했다.
- 첫 펀테스트는 슬라임 1마리와 가마솥 2개로 3분 안에 마법서 8장을 만드는 음성 명령·타이밍 테스트다.
- 주문, 서빙, 매출, 성장 선택과 스탯 차이는 첫 펀테스트에서 비활성화하며 현재 식당 코드는 이전 프로토타입으로 기록한다.

## [2026-07-24] implementation | 터진다! 슬라임 공방 전환

- 기존 시연을 호환 모드 없이 말랑 1마리, 솥 2개, 마도서 1종인 3분 펀테스트로 교체했다.
- 약초부터 납품까지 원자 명령 7종, FIFO 큐, 속도 기반 BFS 이동, 소지품과 두 솥의 독립 5초 타이머를 구현했다.
- 맵과 HUD에 재료·마도서 위치, 슬라임 소지품, 솥 상태, 현재 작업과 큐를 표시한다.
- Gemini와 CLI가 게임 코어의 동일한 actor/action/target 허용 목록을 사용하도록 변경했다.
- 테스트 11/11, CLI 결정론, lint, build가 통과했다. 브라우저에서 한 권의 전체 제작·납품, 두 번째 솥 선택, 이동·IDLE·소지 표시와 콘솔 오류 없음을 확인해 `PASS`로 판정했다.

## [2026-07-25] implementation | 플레이테스트 기록 백엔드

- 배포 대상을 Vercel로 옮기지 않고 기존 Cloudflare Workers + D1 스캐폴드를 그대로 사용하기로 했다.
- `db/schema.ts`에 `playtest_sessions` 테이블을 추가하고 마이그레이션 SQL을 생성했다.
- 결과, 납품 수, 목표, 경과 시간, 음성·버튼 명령 수, 음성 실패 수, 평균 신뢰도를 저장한다.
- `app/api/sessions/route.ts`가 저장(POST)과 집계·최근 기록 조회(GET)를 제공한다.
- 신뢰 경계 검증을 `game/session.ts`로 분리해 D1 없이 테스트하며, 위조된 요약은 400으로 거부한다.
- 클라이언트는 한 판이 끝날 때 지표를 한 번만 전송하고 재시작 시 초기화한다.
- `.openai/hosting.json`의 `d1`을 `DB`로 켜 바인딩을 활성화했다.
- 테스트 12/12, lint, 타입체크 신규 오류 없음, CLI 결정론을 확인했다.

## [2026-07-25] implementation | 한 타일 가구·가까운 솥 자동 선택·호버 카드

- 납품대, 약초 상자, 양피지 상자, 솥이 각각 정확히 한 타일만 차지하도록 맵을 정리하고 작업 타일을 가구와 상하좌우 인접 바닥으로 보정했다.
- 솥 작업 명령의 targetId에 null을 허용했다. 실행 시점에 작업 상태가 맞는 솥을 우선해 BFS 경로가 가장 짧은 솥을 고르고, 동률이면 고정 순서로 결정론을 지킨다. Gemini 스키마·프롬프트, CLI, 디버그 버튼(기본값 "가까운 솥 자동")이 같은 규칙을 쓴다.
- 말랑에 커서를 올리면 일러스트, 이름, 스탯 4종(작업 속도·이동 속도·청력·집중력) 0~5칸 게이지, 받은 버프를 보여 주는 호버 카드를 추가했다. 스탯 레벨은 표시용이며 수치 연동은 펀테스트 범위 밖이다.
- 게임 루프가 고정 50ms 대신 실측 경과 시간을 델타로 넘기도록 고쳐 백그라운드 탭 스로틀에도 게임 시간이 벽시계와 함께 흐르게 했다.
- 테스트 14/14, lint, 타입체크 신규 오류 없음. 브라우저에서 한 타일 렌더링, 호버 카드, 미지정 ADD_HERB의 왼쪽 솥 자동 배정과 MIX의 상태 우선 배정을 확인했다.

## [2026-07-25] implementation | 스탯 활성화·슬라임 4종 선택·참고 이미지 배치

- 스탯 문서의 중앙 레벨 표를 코어에 적용했다. 작업 속도는 작업 시간 배율, 이동 속도는 타일/초, 청력은 플레이어와의 명령 가능 거리, 집중력은 큐 한도로 동작한다.
- 너드·날쌘·쫑긋·일꾼 4종을 문서의 기본 레벨 조합으로 구현하고, 시작 전 선택 화면에서 서로 다른 1~3마리를 골라 이름으로 지휘한다.
- 청력 밖 명령은 NOT_HEARD, 명령 과다는 TOO_COMPLEX, 큐 초과는 QUEUE_FULL로 거절하고 슬라임 위 아이콘과 이벤트로 알린다. 팀 명령도 슬라임마다 개별 판정한다.
- 맵과 UI를 참고 이미지 기준으로 재배치했다. 좌측 남은 시간·획득 골드 배지, 우측 패널, 상단 테이블, 좌우 벽의 약초·양피지 상자, 하단 납품대, 노랑·주황 솥, 슬라임 타원과 중앙 붉은 플레이어, 청력 원 표시.
- 납품당 100G를 적립하는 표시용 골드를 추가했다. 승리는 여전히 8권 납품이다.
- CLI에 --slimes, --player, PLAYER 토큰, 액터 접두사(actor.ACTION)를 추가했고 기본은 플레이어가 지시 대상을 따라다니는 모드다.
- Gemini 라우트가 스쿼드를 받아 이름→actorId 매핑과 스키마 enum을 제한한다.
- 테스트 18/18 (스탯 파생, 작업 배율, 청력 경계, 집중력 거절, 병렬 큐, 결정론, CLI). lint, 타입체크 신규 오류 없음. 브라우저에서 선택 화면, 이미지 배치, 청력 원, NOT_HEARD, 호버 카드, 결과 오버레이와 콘솔 무오류를 확인했다.

## [2026-07-26] ingest | 음성인식 시스템 논의 정리

- `raw/voice-command-discussion-summary-revised.md`에 사용자 원문을 보관했다.
- 음성인식은 핵심 조작이며 그룹·스쿼드 명령은 기각, 개별 슬라임 지정이 기본이다.
- 펀테스트 피드백 요구(§13): 인식된 문장, 해석된 슬라임·행동·대상, 성공 여부, 실패 구분.
- 문서 §9·§12의 "청력·집중력 고정, 슬라임 1마리" 제안은 2026-07-25 사용자 지시(스탯 활성화, 1~3마리 선택)와 충돌한다. 사용자 지시를 우선하고 충돌은 project.md 열린 질문에 기록했다.

## [2026-07-26] implementation | 음성 피드백 카드와 해석 실패 구분

- Gemini 스키마에 들은 문장 transcript와 해석 불가 status UNKNOWN을 추가했다. transcript는 표시용이며 명령 해석에 사용하지 않는다.
- UNKNOWN이면 서버가 422로 이유와 문장을 돌려주고 게임은 실행하지 않는다.
- UI에 마지막 음성 명령 카드를 추가했다. 인식 실패/해석 실패(들은 문장 표시)/접수(해석된 슬라임·행동·대상 표시)를 색으로 구분하고, 상태 불가는 기존 최근 상황·알림 아이콘이 담당한다.
- validateEnvelope가 transcript 문자열 타입을 검증하고 UNKNOWN을 실행하지 않는 테스트를 추가했다.
- 테스트 18/18, lint, 타입체크 신규 오류 없음. 키 없는 503과 잘못된 actors 400 응답, 선택 화면→게임 진입, 콘솔 무오류를 확인했다. Gemini 실호출 경로는 GEMINI_API_KEY가 없어 검증하지 못했다.

## [2026-07-26] implementation | 첫 직원 1마리·레시피 안내·화면 정리

- 선택 화면을 첫 직원 1마리 선택으로 바꿨다. 코어와 CLI의 1~3마리 지원은 후속 다마리 테스트용으로 유지한다.
- 우측 패널 맨 위에 마도서 레시피 5단계와 "3분 안에 8권 납품" 목표를 항상 표시한다.
- 디버그 버튼을 기본 접힘(details)으로 바꿔 화면 복잡도를 줄였다.
- 첫 슬라임 스폰을 플레이어 옆 (7,6)으로 옮겨 시작 직후 명령이 청력 범위 안에 들어온다. 청력 테스트 좌표를 새 스폰 기준으로 갱신했다.
- 테스트 18/18, lint, 타입체크 신규 오류 없음. 브라우저에서 1마리 선택, 레시피 패널, 시작 직후 "쫑긋 큐에 GET_HERB 추가" 접수를 확인했다.
- 사용자 요청으로 ponytail 플러그인(DietrichGebert/ponytail)을 user 스코프로 설치했다. 새 세션부터 훅이 활성화된다.

## [2026-07-26] fix | 병합본 배포 전 검증

- 큰 tick에서 새 솥 타이머가 이전 경과 시간까지 차감하던 문제를 50ms 고정 양자로 제한했다.
- 플레이테스트 세션의 목표·승패·납품 수·경과 시간 일관성을 검증한다.
- Gemini 응답 actor를 이번 판에 선택된 슬라임 1~3마리로 다시 제한한다.
- README의 선택 안내를 실제 UI와 같은 첫 직원 1마리로 고쳤다.
- 회귀 테스트 19/19, lint, build를 `PASS`로 판정했다.

## [2026-07-27] implementation | 슬라임 SVG 아트·작업 애니메이션·나무 배경·폰트

- `public/home/green-slime.svg`를 색·방향 파라미터를 받는 생성기(`app/slime-art.ts`)로 옮겨 선택 화면, 호버 카드, Phaser가 같은 그림을 쓴다.
- 종류별 색 4종과 방향 4종을 만든다. 좌우는 얼굴을 옮기고 위를 보면 등만 보인다. Phaser는 이동 델타로 방향을 판정해 텍스처를 바꾼다.
- 몸과 눈을 분리했다. 몸은 tween(대기 숨쉬기, 걷기 통통, 젓기 좌우 흔들림, 집기 squash), 눈은 3.2~5.2초 주기로 감은 텍스처를 140ms 교체해 걷는 중에도 깜빡인다.
- 손에 드는 것이 없어 구분이 안 되던 `MIX`에 젓는 몸짓과 솥 소용돌이를 붙였다. 소용돌이는 솥이 조합·각인 중일 때만 돈다.
- 선택 화면과 호버 카드의 SVG에는 원본 CSS 애니메이션을 되살렸다. Phaser는 SVG를 정지 화면으로 구워 무시한다.
- 배경을 홈 화면(bg.png + #2f1500)과 같은 나무 공방 톤으로 바꿨다. 바닥은 어두운 가로 판자, 벽은 밝은 세로 널로 결을 반대로 줘 통행 여부가 읽히게 했고 솥·납품대 글로우와 떠오르는 불씨를 얹었다.
- 본문 폰트를 Jua로 바꾸고 Phaser 텍스트에도 적용했다.
- 테스트 20/20, lint, 타입체크 신규 오류 없음. 브라우저에서 선택 화면 4색·CSS 애니메이션, 게임 내 렌더, 걷기 납작 프레임, 나무 배경 대비, Jua 적용, 콘솔 무오류를 확인했다. 젓기 몸짓(800ms)과 솥 소용돌이(5초)는 스크린샷 왕복이 더 느려 순간을 잡지 못했다.

## [2026-07-27] implementation | 게임 중심 UI와 한 걸음 안내

- 화면을 웹 문서 레이아웃에서 게임 화면으로 바꿨다. 제목과 FUN TEST 배지, 좌우 사이드 패널을 없애고 캔버스가 화면을 채우도록 `.stage-frame` 하나로 정리했다.
- HUD를 캔버스 위 오버레이 칩으로 옮겼다. 상단 중앙에 시간·납품·골드, 좌상단에 솥 상태, 우상단에 슬라임 소지·큐, 하단에 안내와 최근 상황.
- 레시피 5단계를 항상 보여 주는 대신 `game/hint.ts`가 소지품과 솥 상태로 다음 한 걸음만 계산해 크게 보여 준다. 첫 화면은 "약초를 가져오세요"와 예시 발화 "쫑긋아, 약초 가져와"로 시작한다.
- 말하기 버튼을 원형으로 키워 하단 우측에 두고 녹음 중에는 맥동한다. 처음에는 하단 중앙에 두었더니 납품대를 가려 오른쪽으로 옮겼다.
- 디버그 버튼은 우하단 작은 토글 안으로 접었다.
- 테스트 21/21(다음 할 일 안내 1건 추가), lint, 타입체크 신규 오류 없음. 브라우저에서 선택 화면, 게임 화면 장악, 약초를 든 뒤 안내가 "솥에 약초를 넣으세요"로 바뀌는 것을 확인했다.
## [2026-07-27] implementation | 상시 음성 인식

- 공방 가동 시작과 함께 마이크 권한을 요청하고 라운드 종료까지 음성 대기를 유지한다.
- 브라우저 음량 감지로 발화를 시작하고 0.8초 침묵 또는 8초 상한에서 녹음을 자동 전송한다.
- 침묵과 0.2초 미만의 순간 소음은 Gemini에 보내지 않으며 해석 중에는 중복 요청을 막는다.
- 테스트 19/19, 변경 파일 lint, build가 통과했다.

## [2026-07-27] implementation | 홈과 게임 화면 분리

- `raw/home/`의 참고 이미지와 에셋을 근거로 나무 공방 홈을 구성했다.
- 홈은 `/`, 슬라임 선택과 게임은 `/game`으로 분리하고 양쪽에 이동 수단을 뒀다.
- 시작·설정 메뉴와 데스크·솥·슬라임을 반응형으로 배치했다.
- 초록 슬라임 SVG의 외피·몸체·얼굴·눈 벡터 그룹에 숨쉬기, 눌림, 눈깜빡임 모션을 적용하고 동작 줄이기 설정에서는 멈춘다.
- 완성된 홈의 로고·목재 공방·슬라임 구성을 링크 공유용 소셜 카드에도 반영했다.
- 데스크톱과 390px 모바일, 메뉴 왕복, SVG 프레임 변화, 테스트 19/19, lint, build를 확인했다.

## [2026-07-27] implementation | 홈 v2 SVG와 배경 반영

- `raw/home v2/`의 배경과 SVG 로고·버튼·포션 선반·가마솥으로 홈 에셋을 교체했다.
- SVG의 실제 도형 경계에 맞춰 불필요한 내부 여백을 줄이고 로고와 메뉴를 화면 중앙 기준으로 배치했다.
- 포션 기포와 가마솥 수축·팽창 모션을 추가하고 동작 줄이기 설정에서는 멈춘다.
- 테스트 19/19, 변경 파일 lint, build, 브라우저 프레임 변화를 확인했다. 전체 lint는 기존 `.claude/worktrees/` 빌드 산출물을 검사해 실패한다.

## [2026-07-27] fix | 홈 레퍼런스 좌표와 SVG 패딩 보정

- 314×225 레퍼런스 좌표계를 화면 중앙 스테이지로 사용해 로고·버튼·선반·가마솥·슬라임의 위치와 크기를 맞췄다.
- 슬라임 SVG의 잘린 viewBox와 루트 크기 비율이 달라 생긴 위아래 여백을 제거했다.
- 원본 SVG의 흰 외곽 도형은 변경하지 않고 레퍼런스 표시 크기로 배치해 화면상 패딩과 굵기를 유지했다.
- 테스트 19/19, 변경 파일 lint, SVG 파싱, build, 브라우저 화면 비교를 확인했다.

## [2026-07-27] fix | 원본 홈 레퍼런스 명도와 겹침 반영

- `raw/home/home_reference.png`의 배경색에 맞춰 공방 배경을 어둡게 보정했다.
- 시작·설정 SVG는 가로 폭이 아니라 레퍼런스의 세로 길이를 기준으로 크기를 정했다.
- 슬라임을 키워 화면 아래와 가마솥을 자연스럽게 가리도록 배치했다.
- 로고 SVG의 불필요한 투명 여백을 줄여 로고와 버튼의 화면상 외곽 패딩을 같은 크기로 맞췄다.

## [2026-07-27] fix | 홈 소품 명도와 가마솥 거품

- 슬라임 SVG의 위쪽 캔버스를 넓혀 세로로 늘어나는 순간에도 외곽선이 잘리지 않도록 했다.
- 포션 테이블을 키우고 테이블·가마솥의 명도와 채도를 낮춰 배경 소품처럼 보이게 했다.
- `raw/home v2/cauldron-bubble.svg`를 가마솥 위에서 서로 다른 박자로 상승하며 사라지는 거품 세 개에 사용했다.
- 동작 줄이기 설정에서는 소품 애니메이션을 멈춘다.

## [2026-07-27] fix | 가마솥 거품 레이어 보정

- 거품 생성 위치를 가마솥 입구 오른쪽에 모으고 크기를 키웠다.
- 거품은 가마솥보다 뒤 레이어에 놓아 솥 안에서 올라오는 것처럼 가려졌다가 나타난다.
- 배경 소품과 어울리는 어두운 청회색을 유지하면서 생성 위치가 보이도록 불투명도를 보정했다.

## [2026-07-27] fix | 가마솥 거품 위치와 외곽 여백 보정

- 거품 묶음의 중심을 가마솥 입구 중앙으로 옮겼다.
- 거품의 흰색 외곽선을 줄여 다른 홈 에셋의 외곽 여백과 비슷하게 맞췄다.

## [2026-07-27] fix | Claude 병합 후 게임 화면 복구

- 병합 과정에서 빠진 홈·선택·게임 HUD 스타일을 복구했다.
- 상시 음성 인식과 충돌하던 수동 마이크 버튼을 상태 표시로 바꿨다.
- 선택 카드 슬라임 SVG에 상단 여유를 추가해 숨쉬기 모션이 잘리지 않게 했다.

## [2026-07-28] ingest | 자원·제작 시스템 기획

- `raw/slime-workshop-resource-production-design.md`를 현재 기획 근거로 연결했다.
- 핵심 규칙은 약초의 색이 결과물 색을, 목적지가 물약/스크롤 형태를
  결정하는 것이다. 명령 단위는 집기와 투입을 합친 `물품 + 목적지`다.

## [2026-07-28] implementation | 자원 생산·주문·직접 작업

- 붉은·파란 약초 소환진, 양조기, 마법 테이블, 제출대, 쓰레기통과
  6종 물품을 게임 코어와 Phaser 화면에 반영했다.
- 슬라임은 물품 위치와 목적지까지 이동하며 독립 FIFO 큐를 실행하고,
  소지품·설비 재고·작업 상태를 화면에 표시한다.
- 주문 완료 시 다음 주문이 이어지고, 설비 공용 재고 한도와 폐기를
  코어에서 검증한다. 10초 간격 주문 유입은 후속 범위로 남았다.
- 플레이어 E키 직접 작업, 발화 음량 기반 청취 범위, 로컬 문구 매칭과
  Gemini 폴백을 추가하고 상시 음성 인식을 유지했다.
- 코어·CLI 테스트 18개와 프로덕션 빌드를 통과했다.

## [2026-07-28] fix | 설비 맵 완성·뭉개진 발음 사전

- 이전 마도서 맵의 타일 문자와 충돌해 파란 소환진·마법 테이블 뒤에만
  남던 사각 가구 렌더링을 제거했다.
- 소환진, 양조 솥, 마법 테이블, 제출함, 쓰레기통을 서로 다른 Phaser
  실루엣으로 그리고 각 설비 앞 작업 위치를 표시했다.
- 너드·날쌘·쫑긋·일꾼의 유사 발음 목록과 색·물품·목적지의 흔한
  STT 오인식 표현을 로컬 사전에 추가했다. Gemini에도 같은 이름
  별칭 목록을 전달한다.
- 뭉개진 문장 로컬 매칭과 오디오→Gemini 요청 경계를 포함한 테스트
  19개, 변경 파일 lint, 프로덕션 빌드와 Chrome 런타임을 확인했다.
- Chrome은 상시 `듣는 중` 상태까지 진입했다. 자동화 음성은 echo
  cancellation으로 실제 마이크에 재입력되지 않아 사람 발화 인식은
  수동 확인이 필요하다.

## [2026-07-28] plan | 음성 사전·푸시투토크·공방 맵 보정

- 사용자 요청에 따라 `좀 끄자`→쫑긋, `양초`→약초의 로컬 사전 보정,
  상시 청취에서 스페이스바 푸시투토크로의 전환, 고정 16×10 공방의
  비기능적 시각 보정을 계획했다.
- 설비 좌표·작업 타일·BFS·자원과 레시피는 바꾸지 않으며, 상세 완료
  조건은 `wiki/plan-voice-push-to-talk-map.md`에 기록했다.

## [2026-07-28] implementation | 음성 사전·스페이스바 푸시투토크·공방 보정

- 로컬 사전에 `좀 끄자`(쫑긋), `양초`(약초)를 추가하고 해당 조합이
  쫑긋의 붉은 약초→양조기 명령으로 매칭되는 회귀 테스트를 추가했다.
- 라운드 시작의 상시 청취를 제거했다. 스페이스바 누름으로 STT 또는
  녹음을 시작하고, 뗄 때 한 번 종료·해석한다. 입력 요소에서는 동작하지
  않으며 기본 스페이스 스크롤을 막는다.
- 16×10 타일·설비·작업 위치·경로 규칙은 유지한 채 천장 보, 작업 러그,
  선반·상자와 조명을 Phaser 배경에 더했다.
- 테스트 19/19, 변경 파일 lint, 프로덕션 빌드를 통과했다. 브라우저에서
  공방 장식과 HUD의 비겹침, 라운드 시작 후 `마이크 준비` 상태를 확인해
  `PASS`로 판정했다. 실제 마이크 권한 요청은 자동화하지 않았다.

## [2026-07-28] plan | 음성 인식 실험실

- 게임과 분리된 `/voice-lab`에서 대상과 유효 작업 조합을 고르고 실제로
  발화해 브라우저 STT, 로컬 사전, Gemini 결과를 비교하는 계획을 세웠다.
- 기대값과 각 단계 결과, 필드별 일치 여부를 D1에 저장하고 최근 기록 표와
  CSV로 추출한다. 원본 오디오·자동 사전 수정·차트는 1차 범위에서 제외한다.
- 상세 범위와 완료 조건은 `wiki/plan-voice-lab.md`에 기록했다.

## [2026-07-28] implementation | 음성 인식 실험실

- `/voice-lab`에 대상 4종과 코어가 허용하는 작업 16종 선택, 스페이스바
  푸시투토크, 실시간 STT, 기대값 대비 로컬 사전·Gemini 필드 비교를
  구현했다.
- 신뢰 경계에서 입력을 재검증한 뒤 `voice_test_results` D1 테이블에
  성공·불일치·인식 실패·API 실패를 같은 형식으로 저장하고, 최근 500건
  JSON 조회와 UTF-8 CSV 다운로드를 제공한다.
- 원본 오디오 저장, 자동 사전 수정, 차트와 새 의존성은 추가하지 않았다.
- 파서 테스트 20/20, 변경 파일 lint, 프로덕션 빌드, 로컬 D1 저장·조회·
  CSV와 Gemini 실제 API 호출을 통과했다. Chrome에서 페이지와
  스페이스바 종료 저장까지 확인했으며, 사람 발화 검증 뒤 최종 판정한다.

## [2026-07-28] verification | 음성 인식 실험실

- Chrome에서 사람이 스페이스바를 누른 채 실제 발화하고 떼는 전 과정을
  검증했다. 브라우저 STT는 `붉은 양초 양쪽에`(신뢰도 약 87%, 2.0초)로
  기록했다.
- 기대값 `쫑긋 / 붉은 약초 / 양조기`와 비교해 로컬 사전은
  `너드 / 붉은 약초 / 양조기`로 대상만 불일치, Gemini는 `UNKNOWN`과
  이유를 반환했다. STT 원문, 기대값, 단계별 필드와 일치 여부가 같은
  D1 행에 유실 없이 저장되고 최근 기록에서 조회됐다.
- 입력 없음 `SKIPPED`, 로컬·Gemini 일치 `OK`, 실제 발화 불일치
  `UNKNOWN`, 명령 API 장애 `ERROR` 네 경로와 UTF-8 CSV를 확인했다.
  테스트 20/20, 변경 파일 lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 음성 실험실 매칭 근거·결과 정리

- 확정 원문과 Claude 구현을 대조해, 브라우저 STT 불일치 텍스트를
  Gemini에 다시 보내는 폴백이 원래의 오디오 직접 해석 원칙과
  어긋남을 확인했다.
- 텍스트 Gemini 폴백 제거, 감지 표현→표준 의미 표시, 확인 후 결과 한 건
  삭제를 계획했다. 상세 범위는
  `wiki/plan-voice-lab-diagnostics.md`에 기록했다.

## [2026-07-28] implementation | 음성 실험실 매칭 근거·결과 정리

- 사전 검사기가 대상·색상·물품·목적지별 감지 표현, 표준 의미와 기본
  대상 여부를 반환하도록 했다. 저장 원문에 현재 사전을 다시 적용하므로
  마이그레이션 없이 과거 기록과 CSV에도 매칭 근거가 표시된다.
- 브라우저 STT는 로컬 사전까지만 사용하고, `/api/command`는 텍스트
  요청을 400으로 거부한다. Web Speech 미지원 시 원본 오디오를 Gemini로
  보내는 allowlist 경로는 유지했다.
- 최근 기록에 `삭제 → 정말 삭제 / 취소`의 2단계 정리 동작을 추가하고,
  API는 인증과 양의 정수 ID를 확인한 뒤 한 행만 삭제한다.

## [2026-07-28] verification | 음성 실험실 매칭 근거·결과 정리

- 실제 `붉은 양초를 양쪽에` 기록에서 `호칭 없음→너드(기본 대상)`,
  `붉은→붉은색`, `양초→약초`, `양쪽→양조기`를 화면·JSON·CSV에서
  확인했다.
- 삭제 API의 잘못된 ID 400, 없는 ID 404, 합성 행 삭제 200을 확인했다.
  UI 첫 클릭은 데이터를 유지한 채 `정말 삭제/취소`만 표시하고 취소 뒤
  원상 복귀했다.
- 확인창 자동화 과정에서 실제 발화 ID 8이 한 번 삭제됐으나 SQLite에
  남은 원 레코드에서 ID, 시각, 신뢰도, 발화 시간과 모든 판정값을 추출해
  동일하게 복구하고 조회로 확인했다.
- 테스트 21/21, 변경 파일 lint, `git diff --check`, 프로덕션 빌드와
  브라우저 레이아웃을 통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 현재 소지품 폐기 명령

- 사용자 지시에 따라 `그거`, `들고 있는 거`, `가지고 있는 거`를 지목한
  슬라임의 현재 소지품으로 해석한다.
- 즉시 삭제하거나 새 물품을 가져오지 않고, 현재 운반 목적지를
  쓰레기통으로 바꾸어 실제 이동 뒤 폐기한다. 상세 완료 조건은
  `wiki/plan-carried-discard-command.md`에 기록했다.

## [2026-07-28] implementation | 현재 소지품 폐기 명령

- `그거`, `들고 있는 거`, `가지고 있는 거`, `갖고 있는 거`,
  `손에 든 거`와 폐기 동사를 로컬에서 조합해 대상 슬라임을 찾는다.
- 대상이 물품을 들고 있으면 기존 운반 명령을 같은 물품의 쓰레기통
  운반으로 교체한다. 소지품과 대기열은 유지하고, 쓰레기통 도착 뒤 기존
  폐기 규칙이 물품을 제거한다.
- 소지품 없음, 명령을 듣지 못함, 종료된 공방과 경로 없음은 코어가
  상태를 만들지 않고 이유를 기록한다. 명시적 물품 폐기 동작은 유지한다.
- 사용자가 추가한 사전 별칭은 보존하고, 빈 문자열 별칭이 모든 문장과
  일치하지 않도록 공통 매처에서 무시한다.

## [2026-07-28] verification | 현재 소지품 폐기 명령

- 날쌘이 붉은 약초를 양조기로 운반하는 도중 `날쌘아 그거 버려`를
  적용해 소지품 유지, 목적지 `trash` 전환, 쓰레기통 도착 뒤 소지품
  제거와 양조기 미생성을 확인했다. 빈손에서는 새 물품을 가져오지 않았다.
- 실험실 합성 기록에서 `날쌘→날쌘`, `그거→현재 든 물품`,
  `버려→쓰레기통`, 상태 `CONTEXTUAL`을 확인한 뒤 합성 행만 삭제했다.
- 테스트 22/22, 변경 파일 lint, 프로덕션 빌드를 통과해 `PASS`로
  판정했다.

## [2026-07-28] plan | 현재 소지품 폐기 음성 실험실 적용

- 게임에 추가한 `그거`, `들고 있는 거`, `가지고 있는 거` 폐기 표현을
  음성 실험실의 독립된 해야 할 일로도 선택하고 저장할 수 있게 한다.
- 기존 기대 문장과 결과 저장 구조를 재사용하며 Gemini 텍스트 재분석은
  추가하지 않는다.

## [2026-07-28] implementation | 현재 소지품 폐기 음성 실험실 적용

- 해야 할 일에 `현재 든 물품 → 쓰레기통`의 `그거`, `들고 있는 거`,
  `가지고 있는 거` 세 항목을 추가하고 선택별 안내 문장을 생성한다.
- 기존 `expected_item`에 `carried`, `expected_phrase`에 실제 안내 문장을
  저장한다. 현재 소지품 폐기로 인식되면 대상·물품·목적지와 전체 일치를
  정형 결과에 기록하며, 별도 테이블과 Gemini 재분석은 추가하지 않았다.

## [2026-07-28] verification | 현재 소지품 폐기 음성 실험실 적용

- 브라우저에서 세 선택지와 `쫑긋아, 그거/들고 있는 거/가지고 있는 거
  버려` 안내 문장의 전환을 확인했고 콘솔 오류가 없었다.
- API 합성 발화 `날쌘아 가지고 있는 거 버려`를 저장해
  `CONTEXTUAL`, 대상·물품·목적지·전체 일치를 확인한 뒤 합성 행만
  삭제했다.
- 테스트 23/23, 변경 파일 lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 현재 소지품 목적지 변경 일반화

- `그거/들고 있는 거/가지고 있는 거`를 폐기뿐 아니라 양조기·마법
  테이블·제출대 목적지에도 적용한다. `테이블`은 마법 테이블 약칭으로
  기존 사전을 재사용한다.
- 실제 소지품에 허용된 경로인지는 게임 코어가 검증하고, 음성 실험실에는
  세 지시어와 네 목적지의 열두 조합을 제공한다.

## [2026-07-28] implementation | 현재 소지품 목적지 변경 일반화

- 맥락 명령 매처가 대상과 함께 양조기·마법 테이블·제출대·쓰레기통
  목적지를 반환하도록 일반화했다. 게임은 현재 소지품을 새로 집지 않고
  해당 작업 타일로 보내며 기존 투입·제작·폐기 규칙을 그대로 사용한다.
- 코어가 실제 소지품의 허용 경로를 검증해 스크롤을 테이블로 보내는 등의
  잘못된 전환은 현재 작업을 유지한 채 거부한다.
- 음성 실험실에는 세 지시어와 네 목적지의 열두 조합을 추가하고, 기존
  `carried` 기대값과 결과 저장 구조를 재사용했다.
- 사용자 사전 별칭은 보존했다. 약초 정규식의 빈 대안과 `가져와`까지
  테이블로 잡는 과잉 매칭만 제거했다.

## [2026-07-28] verification | 현재 소지품 목적지 변경 일반화

- 붉은 약초를 양조기로 운반하던 날쌘에게 `그거 테이블로 가져가`를
  적용해 목적지 전환, 약초 유지, 마법 테이블 도착 뒤 붉은 스크롤 생성을
  확인했다. 스크롤→테이블의 잘못된 전환과 빈손 명령은 거부됐다.
- API 합성 발화에서 `그거→현재 든 물품`, `테이블→마법 테이블`,
  `CONTEXTUAL`과 전체 일치를 확인한 뒤 합성 행만 삭제했다. 브라우저에서
  12개 조합과 `쫑긋아, 그거 테이블로 가져가` 안내 문장을 확인했다.
- 테스트 23/23, 변경 파일 lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 게임 푸시투토크 해석 시점 보정

- Web Speech가 스페이스바를 누르는 도중 자체 종료해도 바로 명령을
  해석하지 않고, 키를 뗀 뒤에만 누적 결과를 해석하도록 한다.

## [2026-07-28] implementation | 게임 푸시투토크 해석 시점 보정

- 스페이스바 누름 때만 STT 결과 버퍼와 음량 측정을 초기화한다. STT가
  누르는 동안 자동 종료하면 버퍼를 유지한 채 다시 듣고, 키를 뗀 뒤의
  종료에서만 한 번 명령을 해석한다.
- STT 오류 뒤에는 재시작 루프에 들어가지 않고, 키를 뗄 때까지 해석도
  하지 않는다.

## [2026-07-28] verification | 게임 푸시투토크 해석 시점 보정

- 테스트 23/23, `app/Game.tsx` lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다. 실제 마이크 발화는 공개 배포본에서 사용자가
  스페이스바를 누른 채 확인할 수 있다.

## [2026-07-28] plan | 음성 실험실 E 푸시투토크

- 사용자의 최신 지시에 따라 `/voice-lab`의 발화 키를 스페이스바에서
  `E`로 바꾼다. 게임 화면의 스페이스바 푸시투토크에는 영향을 주지 않는다.

## [2026-07-28] implementation | 음성 실험실 E 푸시투토크

- 실험실의 누름·뗌 이벤트, 대기·녹음 상태 문구와 키캡을 모두 `E`로
  바꿨다. 입력 요소 선택 중과 키 반복을 무시하는 기존 보호는 유지했다.

## [2026-07-28] verification | 음성 실험실 E 푸시투토크

- 테스트 23/23, 변경 파일 lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 물품 단위 음성 사전

- `프랑스`처럼 색상과 물품으로 나눌 수 없는 STT 결과를 물품 전체
  표현으로 처리하고, 실험실에서 `프랑스→파란 약초` 매칭 근거를 보인다.

## [2026-07-28] implementation | 물품 단위 음성 사전

- 색상·물품 단어 사전보다 먼저 검사하는 물품 전체 표현 사전에
  `프랑스 → blue-herb`를 추가했다. 매칭 근거에는 `프랑스 → 파란 약초`를
  한 항목으로 표시한다.

## [2026-07-28] verification | 물품 단위 음성 사전

- `프랑스 양조기에 넣어`가 파란 약초→양조기 명령으로 생성되고, 물품
  매칭 근거가 `프랑스 → 파란 약초`로 남는 회귀 테스트를 추가했다.
- 테스트 23/23, 변경 파일 lint, `git diff --check`, 프로덕션 빌드를
  통과해 `PASS`로 판정했다.

## [2026-07-28] plan | 명시 물품 우선 음성 해석

- 짧은 현재 소지품 후보는 유지하되, `붉은 약초`처럼 물품이 완성된
  문장은 현재 소지품 명령보다 먼저 해석한다.

## [2026-07-28] implementation | 명시 물품 우선 음성 해석

- 일반 물품 명령을 현재 소지품 명령보다 먼저 검사하도록 순서를 바꿨다.
  따라서 `좀 꺼서 붉은 약초 양주 위에도`는 짧은 `꺼` 후보를 유지한 채
  쫑긋의 붉은 약초→양조기 명령으로 확정된다.
- `중앙스크린` 붉은 스크롤 별칭의 빈 정규식 대안을 제거했다. 별칭 자체는
  유지하면서 모든 문장을 붉은 스크롤로 잡던 과잉 매칭을 막는다.

## [2026-07-28] verification | 명시 물품 우선 음성 해석

- 문제 문장이 쫑긋의 붉은 약초→양조기 명령으로 생성되는 회귀 테스트를
  추가했다. 테스트 23/23, 변경 파일 lint, `git diff --check`, 프로덕션
  빌드를 통과해 `PASS`로 판정했다.
## [2026-07-31] 계획 | 음성 제거와 마우스·키보드 조작 전환

- `raw/조작_방식_변경_명세_수정본.md`를 근거로 좌클릭 선택, 속성 키 선택, 우클릭 이동·설비 상호작용, 음성 기능 제거를 이번 범위로 확정했다.
- 미정인 복수 선택 확장, 진형, 작업 예약 큐, 동시 작업 인원, 카메라, 죽음 발생 규칙은 범위에서 제외했다.
## [2026-07-31] 구현 및 검증 | 음성 인식 제거와 포인터 조작 전환

- `조작_방식_변경_명세_수정본.md`에 따라 음성 인식 런타임/API/테스트 화면을 제거하고, 좌클릭 선택·우클릭 이동 및 상호작용·QWER 속성 선택·Space 전체 선택을 구현했다.
- 선택한 슬라임의 이동은 기존 이동 속도와 경로 탐색을 사용하며, 상호작용 대상의 재료가 없으면 `WAITING` 상태로 기다렸다가 재료가 생기면 재개한다.
- 검증 결과: **PASS**. `npm test` 12/12, `npm run lint`, `npm run build`, 브라우저에서 선택·이동·상호작용·선택 해제를 확인했다.
## [2026-07-31] 계획 | 상호작용 코어 완성

- `다음_개발_작업_상호작용_코어.md`를 근거로 복수 선택 상호작용, 단일 작업자, 속성 거부, 재료 대기·자동 재개, 진행도, 버섯 구이 제작·제출 흐름의 범위와 완료 조건을 확정했다.
- 추가 레시피, 작업 예약 큐, 이동 진형, 업그레이드와 죽음·부활은 이번 범위에서 제외했다.
## [2026-07-31] 구현 및 검증 | 상호작용 코어 완성

- 복수 선택 오브젝트 이동, 가장 먼저 도착하는 작업 가능 슬라임 판정, 불 속성 조리 거부, 단일 작업자 잠금, 재료 부족 대기·자동 재개를 구현했다.
- 조리 도구의 대기·재료 부족·작업 중·작업 완료 상태와 작업자·진행도, 버섯 상자부터 버섯 구이 제출까지의 흐름을 화면과 코어에 연결했다.
- `play-local.sh`가 개발 서버를 시작하고 준비된 `/game`을 브라우저로 열며 종료 시 서버를 정리한다.
- 검증 결과: **PASS**. 코어 테스트 13/13, 린트, 프로덕션 빌드, 셸 구문·실행 권한, 브라우저 실제 조작과 오류 로그를 확인했다.
## [2026-08-01] 계획 | 공방에서 식당으로 테마 전환

- 새 테마 명세와 음악 3종, 물 슬라임 SVG를 근거로 속성 ID·현재 코어 데이터·UI·로그·배경을 식당 표현으로 교체하는 범위를 확정했다.
- 추가 요리, 설거지, 화재·소화, 발전기와 신규 주방 아트 제작은 구체 규칙이나 에셋이 없어 이번 범위에서 제외했다.
## [2026-08-01] 구현 및 검증 | 식당 테마 전환

- 코어 ID와 스탯을 물·불·번개·땅 슬라임으로 교체하고, 활성 데이터를 버섯·버섯 구이와 재료 상자·조리 도구·음식 제출대·쓰레기 처리 공간으로 축소했다.
- 홈과 게임 화면의 공방 전용 표현을 비활성화하고 식당 문구·주방 설비·음식 주문 UI로 교체했다.
- 새 물 슬라임 SVG와 홈·일반 영업·30초 이하 마감 음악을 공개 에셋으로 연결했다.
- 검증 결과: **PASS**. 결정론 테스트 12/12, 린트, 프로덕션 빌드, 브라우저 식당 화면·속성 선택·조리 거부·에셋 표시와 오류 로그를 확인했다.
## [2026-08-01] 계획 | 홈·물 슬라임·이동·음악 설정 보정

- 새 식당 로고와 사용자 피드백을 근거로 홈 에셋 배치 복원, 위쪽 음악 설정 탭, 물 슬라임 크기·탄성·방향 보정, 유클리드 직선 이동, 인게임 이름·상태 목록 제거 범위를 확정했다.
- 장애물 내비게이션 메시, 충돌·진형, 오디오 크로스페이드는 이번 범위에서 제외했다.
## [2026-08-01] 구현 | 홈·물 슬라임·이동·음악 설정 보정

- 홈을 기존 책상·가마솟·거품·초록 슬라임 배치로 복원하고 새 `슬라임 레스토랑` SVG 로고와 메타데이터를 적용했다.
- 홈과 인게임 설정 버튼에서 상단 탭을 열어 음악을 켜고 끄거나 음량을 조절하며, 브라우저 이동 후에도 값을 유지하도록 했다.
- 물 슬라임 SVG의 불필요한 여백을 표시용 본에서만 잘라 선택지 크기를 맞추고, 상하 탄성 애니메이션을 추가했다. 인게임에서는 네 속성이 공통 4방향 아트 경로를 사용한다.
- 타일 BFS 경로와 웨이포인트 상태를 제거하고 클릭 좌표로 x·y가 동시에 변하는 유클리드 직선 이동을 적용했다. 인게임 이름과 우측 상태 목록도 제거했다.

## [2026-08-01] 검증 | 홈·물 슬라임·이동·음악 설정 보정

- 검증 결과: **PASS**. 코어 테스트 13/13, 린트, 프로덕션 빌드를 통과했다.
- 브라우저에서 홈 복원 배치·새 로고, 위에서 열리는 설정 탭, 음악 즉시 긄/재생, 물 슬라임 카드 크기, 인게임 설정·상태 목록 제거를 확인했다.
- 대각선에서 x·y가 동시에 변하고 실제 클릭 좌표에 도착하는지, 물 슬라임의 좌·우 방향 아트와 30초 러쉬 소스 경계를 회귀 테스트로 확인했다.

## [2026-08-01] 수정 및 검증 | 비주얼 반응형 보정

- 게임 홈 링크를 왼쪽 위 탐색 영역으로 옮기고 주문 HUD를 내려, 선택 카드와 인게임 조작 안내를 가리던 겹침을 제거했다.
- 761~900px 데스크톱 폭에서는 슬라임 선택 카드를 4열로 압축해 800×600에서도 네 카드와 시작 버튼을 한 화면에 표시한다.
- 물 슬라임 SVG 루트 크기를 viewBox 비율과 맞춰 다른 속성과 같은 표시 크기로 보정했다. 테스트 13/13, 린트, 빌드, SVG 파싱과 1440×900·1024×768·800×600 브라우저 검증을 통과해 `PASS`로 판정했다.

## [2026-08-01] 계획 | 충돌·깊이·설정 일시정지 보정

- 유클리드 이동을 유지하며 고정 설비만 우회하는 최소 충돌 범위를 정했다.
- y좌표 깊이 정렬과 중앙 설정 모달, 게임 정지 후 `3 → 2 → 1` 재개를 완료 조건으로 정했다.
- 슬라임 간 물리와 동적 장애물은 이번 범위에서 제외했다.

## [2026-08-01] 구현 및 검증 | 충돌·깊이·설정 일시정지 보정

- 고정 설비와 슬라임 크기를 합친 히트박스와 연속 좌표 우회 경로를 추가했다.
- 슬라임·소지품·선택 링과 설비의 깊이를 y좌표에 맞춰 갱신한다.
- 홈·게임 설정을 중앙 모달과 전체 암전으로 바꾸고, 게임 설정 중 코어·Phaser를 멈춘 뒤 `3 → 2 → 1` 후 재개한다.
- 검증 결과: **PASS**. 코어 테스트 13/13, lint, 프로덕션 빌드와 `git diff --check`가 통과했다. 브라우저에서 중앙 좌표·암전, 설정 중 시간 정지, 카운트다운 후 시간 재개를 확인했다.

## [2026-08-01] 계획 | 주문 시스템·화재 시스템

- 담당 분리 문서를 근거로 주문 목록·제출 판정·라운드 판정과 조리 도구
  방치 발화·전파·물 슬라임 진화의 범위를 확정했다.
- 명세 18항의 미정 사항 8개는 규칙을 확정하지 않고 `orderConfig`,
  `fireConfig` 설정값으로 분리했다. 기본값은 현재 게임 동작을 유지한다.
- 화재 진압 후 복구 시간은 규칙이 없어 설정도 코드도 만들지 않고 미정
  항목으로 남겼다.
- 새 레시피, 조리 과정, 보상·돈, 바닥 화재, 소화기, 자동 소화 설비는
  범위에서 제외했다.

## [2026-08-01] 구현 | 주문 시스템·화재 시스템

- 주문을 `{ id, foodId, targetCount, submittedCount }` 목록으로 바꾸고
  라운드 시작 시 주입할 수 있게 했다. 주입 목록은 코어에 들어가기 전에
  ID 중복·음식 ID·수량을 검증한다.
- 제출 판정을 음식 이름이 아니라 `ItemId` 대조로 하고, 주문에 없는
  음식은 `orderConfig.invalidSubmission`대로 거부하거나 제거한다.
- 제출대 경로를 미리 막던 `canUseStation`을 고쳐 제출 판정을 주문
  시스템이 맡게 했다. 이 게이트 때문에 잘못된 음식이 제출대에 도달하지
  못하던 것이 원인이었다.
- 라운드 종료 판정을 `roundResult`로 분리해 시간 만료 시에도 주문을 다
  채웠으면 성공으로 판정한다.
- 설비별 화재 상태를 추가해 조리 완료 방치 발화, 사용 차단, 인접 설비
  전파, 물 슬라임 5초 진화와 진행도를 구현했다. 진화 중 새 지시를
  받으면 설정에 따라 진행도를 버린다.
- 화재 대상이 아닌 설비에는 화재 상태를 만들지 않고, 인접 판정은 바닥
  타일이 아니라 설비 배치 거리로만 한다.
- HUD에 활성 주문의 음식·제출 수·목표·완료 여부와 조리 도구 화재·진화
  진행도를 표시했다. 게임 제목을 `슬라임 레스토랑`으로 맞췄다.

## [2026-08-01] 검증 | 주문 시스템·화재 시스템

- 검증 결과: **PASS**. 코어 테스트 19/19, 린트, 프로덕션 빌드를 통과했다.
- 새 회귀 테스트 6종: 주문 목록 주입과 진행도, 주문에 없는 음식의 두
  설정 처리, 라운드 성공·실패 판정과 주문 검증, 방치 발화와 사용 차단,
  물 슬라임 5초 진화와 설비 복귀, 인접 전파와 비대상 설비 제외.
- 브라우저에서 주문 HUD 표시, 조리 완료 방치 뒤 화재 발생, 불 슬라임의
  진화 거부, 물 슬라임 진화와 조리 도구 복귀, 시간 만료 실패 판정을
  확인했다.
- 현재 맵에는 화재 대상 설비가 조리 도구 하나뿐이라 실제 플레이에서는
  전파가 일어나지 않는다. 전파는 설정으로 대상을 늘려 검증했다.

## [2026-08-01] 수정 및 검증 | 주문·화재를 충돌·깊이 보정 위로 통합

- `main`에 주문·화재 작업 대신 기준용 베이스 스냅샷이 먼저 합쳐졌다.
  스냅샷과 코덱스의 정식 커밋이 같은 작업의 서로 다른 판이라, 합쳐지면서
  `tests/game.test.ts`에 테스트 두 개가 중복 삽입되고 선언되지 않은
  `state`를 참조하는 줄이 끼어들어 `main`의 테스트가 컴파일되지 않았다.
- 중복된 두 테스트를 제거하고, 그 과정에서 되돌아갔던 설비 히트박스
  우회 검사를 복원했다.
- 검증 결과: **PASS**. 코어 테스트 19/19, 린트, 프로덕션 빌드를 통과했다.
  `main`의 고유 테스트 13개가 모두 남았고 주문·화재 회귀 6개가 더해졌다.

## [2026-08-02] 계획 | 같은 속성 슬라임 중복 영입

- 추가본 명세의 슬라임 영입 화면은 이미 데려온 속성도 다시 고를 수 있다.
- 현재 코어는 `ActorId = SlimeTypeId`라 속성 자체가 키였고, 중복 스쿼드를
  명시적으로 거부했다. 영입이 라운드마다 쌓이므로 스테이지 구조를 짜기
  전에 먼저 바꾼다.
- 점수·랭킹·죽음은 사용자 결정에 따라 범위에서 제외한다.

## [2026-08-02] 구현 | 같은 속성 슬라임 중복 영입

- `ActorId`를 인스턴스 ID(`water-1`, `water-2`)로 바꾸고 속성은
  `ActorState.typeId`에서 읽는다. 코어는 ID를 불투명 키로만 쓰고 있어
  실제 수정은 타입 선언, `initialState`, 스쿼드 검증 세 곳이었다.
- 같은 속성이 둘 이상일 때만 이름에 `2호`처럼 번호를 붙여 로그와 UI에서
  구분되게 했다. 스쿼드 상한은 스폰 자리 수로 유지한다.
- 화면은 스프라이트를 마리 수만큼, 텍스처를 속성 수만큼 만든다. 속성
  키(`Q/W/E/R`)는 그 속성의 모든 마리를 한 번에 고른다.
- CLI 토큰은 인스턴스 ID로 특정 마리를, 속성명으로 그 속성의 첫 마리를
  지목한다.
- `moveActor`에서 `actor` 재할당으로 풀리던 `intent` 좁히기를 지역 변수로
  붙잡았다. 기존 코드의 타입 오류이며 런타임 동작은 같다.

## [2026-08-02] 검증 | 같은 속성 슬라임 중복 영입

- 검증 결과: **PASS**. 코어 테스트 21/21, 린트, 프로덕션 빌드,
  `tsc --noEmit`(strict), `git diff --check`를 통과했다.
- 새 회귀 테스트 2종: 중복 스쿼드의 ID·이름·스폰 분리와 한 마리 지시가
  같은 속성의 다른 마리를 움직이지 않는 것, CLI의 속성명·ID 지목.
- 브라우저에서 물 2·불 2 스쿼드가 각각 독립 스프라이트로 그려지고,
  `Q`가 물 2마리를 `W`가 불 2마리를 동시에 선택하는 것을 확인했다.
  확인용 임시 스쿼드 변경은 되돌렸다.
- 영입 화면이 아직 없어 UI에서는 중복 스쿼드를 만들 수 없다. 스테이지
  구조 작업에서 열린다.

## [2026-08-02] ingest | 추가본 덱 정리

- `raw/추가본.pptx`(15장)의 화면별 내용과 2026-08-02 회의 결정을
  `raw/추가본_정리/`에 텍스트로 정리했다. `.pptx`가 바이너리라 열 때마다
  압축을 풀어야 하는 것을 피하기 위함이다.
- `raw/`는 원문 전용 읽기 전용 규칙이지만, 사용자가 이번 한 번에 한해
  쓰기를 허락했다. 기존 `raw/` 파일은 건드리지 않았고, 예외라는 사실을
  폴더 README에도 남겼다.
- 확정: 화재는 설비 단위(바닥 아님), 정산 합계 자리에 기존 골드 사용,
  슬라임 명칭은 땅, 같은 속성 중복 영입 가능, 스테이지 구조 도입.
- 제외: 점수 시스템, 랭킹, 슬라임 죽음.
- 미정으로 남긴 것: 주문 실수·화재 횟수 카운터와 골드 감액 여부, 스테이지
  정의 필드, 스테이지당 음식 6종 초과 처리, 스쿼드 상한, 화재 발생 시간,
  진압 후 복구 시간, 땅 슬라임 이름과 새싹 아트의 불일치.
- 덱과 기존 담당 문서가 어긋나는 항목(점수·죽음·화재 타일)은 회의 결정을
  우선하되 양쪽을 함께 적어 두었다.

## [2026-08-02] 결정 | 화재 손실 제외와 문서 작성자 표기

- 화재로 인한 골드 차감은 넣지 않고 정산 화면에도 올리지 않는다. 화재의
  페널티는 설비를 쓸 수 없는 것과 물 슬라임 진화 부담뿐이다. 코드는 이미
  그렇게 돼 있어 변경 없이 문서만 갱신했다.
- 정산에 남는 항목은 `주문 성공`과 미정인 `주문 실수` 둘뿐이다.
- 코덱스와 함께 작업하는 저장소라 `raw/추가본_정리/`의 세 문서 머리에
  Claude가 작성한 2차 자료임을 명시했다. 원본 덱과 사용자 판단이
  우선한다는 것도 함께 적었다.
- `스테이지 정의 필드`라는 표현이 설명 없이 쓰여 있어, 스테이지 한 판을
  적는 항목 목록이라는 뜻으로 예시와 함께 풀어 썼다.

## [2026-08-02] 계획 | 스테이지 루프

- 3분 단일 라운드를 여러 스테이지를 이어 도는 구조로 바꾼다. 범위는
  루프가 실제로 도는 것까지이며 소개·로딩·영입 화면은 다음 차례다.
- 스테이지 하나를 `{ id, name, orders, timeLimitMs }`로 적어 스테이지를
  늘릴 때 코드를 건드리지 않는다.
- TIP과 나올 수 있는 음식 목록은 소개 화면에서만 쓰이므로 읽는 곳이 생길
  때 함께 추가한다.
- 주문 실수는 횟수만 세고 골드를 깎지 않는다.

## [2026-08-02] 구현 | 스테이지 루프

- `Stage` 타입과 기본 스테이지 3판을 추가하고, `initialState`가 스테이지에서
  주문과 제한 시간을 가져오도록 했다. 스테이지 목록과 인덱스도 신뢰 경계에서
  검증한다.
- `GameState`에 `stages`, `stageIndex`, `squad`, `misses`를 추가했다.
- 스테이지를 깬 뒤 다음 판을 만드는 `nextStage`를 추가했다. 골드와 스쿼드는
  잇고 주문·시간·설비·화재는 새로 시작한다.
- 결과 화면을 정산 화면으로 바꿨다. 주문 성공 횟수와 획득 골드, 주문 실수
  횟수, 누적 골드를 3열로 보여 주고 `다음 스테이지`/`1-1부터`/`타이틀로`를
  제공한다. HUD에 스테이지 번호와 이름을 표시한다.
- 쓰이지 않게 된 `defaultOrders`를 제거했다.

## [2026-08-02] 검증 | 스테이지 루프

- 검증 결과: **PASS**. 코어 테스트 23/23, 린트, 프로덕션 빌드,
  `tsc --noEmit`(strict)를 통과했다.
- 새 회귀 테스트 2종: 스테이지 클리어 후 골드·스쿼드를 이어 다음 판으로
  넘어가고 마지막 판에서는 넘어가지 않는 것, 주문에 없는 음식 제출이 실수로
  세어지되 골드가 줄지 않는 것.
- 브라우저에서 HUD의 스테이지 표시(`1-1 첫 영업`), 정산 3열 표(성공은 골드,
  실수는 횟수만, 화재·죽음 행 없음), 게임 오버의 `1-1부터`·`타이틀로`를
  확인했다.
- 브라우저 확인 중 **기존 버그를 발견해 고쳤다**: `game/session.ts`가 목표
  5건과 제한 시간 180초를 하드코딩해, 주문 수가 다른 스테이지의 플레이 기록이
  전부 거부됐다. 목표 고정 검사를 없애고 승패와 납품 수만 대조하도록 바꿨으며,
  경과 시간은 위조를 거르는 상한만 남겼다. `app/Game.tsx`가 보내던
  `180_000 - timeLeftMs`도 스테이지 제한 시간 기준으로 고쳤다.
- 스테이지 클리어 화면의 `다음 스테이지` 버튼은 브라우저에서 확인하지 못했다.
  제한 시간 안에 주문을 채우지 못해 두 번 다 게임 오버로 끝났다. 전환 자체는
  회귀 테스트가 덮는다.
## [2026-08-01] 복원 및 검증 | 로컬 실행기와 병합 화면 보정

- 최신 결합 커밋으로 `main`을 fast-forward해 실행 권한이 포함된
  `play-local.sh`를 복원하고, 실제로 `/game` 개발 서버가 열리는지 확인했다.
- 병합 과정에서 중앙 모달 CSS 뒤에 남은 구형 상단 서랍 규칙을 삭제해
  음악 설정 패널이 화면 정중앙에 표시되도록 복구했다.
- 검증 결과: **PASS**. 셸 문법, 코어 테스트 19/19, 린트, 프로덕션 빌드를
  통과했다. 1280×720·1024×768·800×600 홈/인게임 배치, 설정 중 정지와
  재개 카운트다운, 홈·일반 영업 BGM 재생과 세 음원의 상대 음량을 확인했다.

## [2026-08-01] 구현 및 검증 | 게임 오버 BGM 전환

- `raw/GAME OVER.mp3` 원본은 보존하고, 기존 BGM보다 약 22dB 작던 음량을
  게임용 파생본에서 20dB 높여 평균 -19.5dB·피크 -2.8dB로 맞췄다.
- 패배 상태에서만 게임 오버 BGM으로 전환하고 승리·일반·러쉬 음악 경계는
  유지했다.
- 검증 결과: **PASS**. 코어 테스트 19/19, 린트, 프로덕션 빌드를 통과했다.
  브라우저에서 시간 만료 패배를 재현해 60.224초 곡이 저장 음량 35%로
  재생되고 반복 설정이 적용되는지 확인했다.

## [2026-08-01] 구현 및 검증 | 효과음·불 슬라임·client 에셋 정리

- 현재 구현 이벤트에 대응하는 효과음 40개만 `public/sfx`에 추가하고 디코딩
  피크를 -5.32~-4.02dB로 정규화했다. 시스템이 없는 설거지·도마질·쓰레기
  소각 4개는 `raw`에만 보존했다.
- UI 클릭·호버, 라운드 시작·종료, 러쉬 경고, 재료 생성, 운반, 조리 시작,
  음식 제출, 주문 완료·교체, 이동, 발화 원샷·루프와 진압 효과음을 연결했다.
  `A07_food_ready`는 음식 제출 전환에서만 재생한다.
- `fire_slime.svg` 파생본에 탄성 호흡 애니메이션과 모션 감소 대응을 넣고,
  불꽃을 제외한 몸통이 물 슬라임과 약 72×54px로 같아지도록 표시 상자를
  84×72px로 잡았다.
- 전체 참조 검색으로 미사용을 확인한 Next 기본 아이콘 3개, 이전 홈 PNG
  5개, 미참조 `og.png`를 삭제했다. 삭제 파일은 Git으로 복구 가능하다.
- 검증 결과: **PASS**. 코어 테스트 20/20, 린트, 프로덕션 빌드, SVG 파싱과
  1280×720·800×600 브라우저 검증을 통과했다. 실제 조리·발화·진압·제출
  흐름에서 각 효과음 요청과 화재 루프 중지를 확인했으며 콘솔 오류는 없었다.

## [2026-08-01] 계획 | 그릇·테이블·편의 선택·인게임 불 슬라임

- 추가 명세를 근거로 그릇 생성·제출·오염·세척 순환, 한 칸 테이블,
  Shift·드래그·단축키 선택과 ESC 설정 호출 범위를 정했다.
- 미정 수량과 시간은 확정 기획과 분리해 `dishConfig`의 첫 검증값으로
  관리하고 Wiki의 열린 질문으로 유지한다.
- 선택 카드에만 있던 불 슬라임 원본을 Phaser 텍스처와 기존 모션에 연결하고,
  물·불 선택 애니메이션과 불꽃 잘림을 함께 보정한다.

## [2026-08-01] 구현 및 검증 | 그릇·테이블·편의 선택·인게임 불 슬라임

- 고유 ID의 깨끗한 그릇을 생성대에 만들고, 재료/완성 음식 담기, 제출 뒤
  오염, 세척기에 맡기기, 물 슬라임 4초 세척, 재사용까지 연결했다.
- 테이블은 미정인 다중 슬롯 선택을 만들지 않도록 한 칸으로 두고, 재료와
  모든 상태의 그릇을 ID·내용물 손실 없이 인계한다. 땅 슬라임은 그릇만
  두 개까지 운반한다.
- Shift 좌클릭 토글, 빈 바닥 드래그 범위 선택, 기존 단축키 선택과 ESC
  설정 호출을 함께 적용했다. 설정 중 코어와 Phaser가 멈추고 선택은 남는다.
- 원본 불 슬라임 SVG를 Phaser 텍스처로 직접 사용해 몸통 크기를 맞추고
  숨쉬기·걷기·조리 tween과 좌우 반전을 적용했다. 선택 카드에서는 물·불이
  같은 2.8초 탄성 모션을 쓰며 불꽃 위 여백을 늘렸다.
- 설거지 효과음 `A00_washing_dish`를 -4.5dB 피크로 맞춰 세척 시작에
  연결했다. 음식 제출의 `A07_food_ready` 연결은 그대로 유지한다.
- 검증 결과: **PASS**. 결정론 코어 테스트 22/22, lint, 프로덕션 빌드,
  SVG 파싱, 셸 문법, `git diff --check`를 통과했다. 800×600과 1440×900
  브라우저에서 Shift·드래그·ESC, 땅 슬라임 2개 운반, 테이블 인계,
  조리·제출·오염·세척 순환, 불 슬라임 걷기·조리 모션을 직접 확인했다.

## [2026-08-01] 계획 | 홈·게임 전체 화면 레이아웃

- 홈 구성판의 높이 기준 고정 종횡비와 게임의 8:5 제한 프레임이 넓거나
  긴 화면에 빈 웹 배경을 남기는 원인임을 확인했다.
- 홈은 전체 뷰포트 좌표계로 전환하고 게임은 전체 화면 프레임 안에서
  화면 방향에 따라 채움·비율 보존을 나눠 핵심 정보 잘림을 막는다.
- 게임 홈 이동은 상시 버튼에서 설정 모달 내부 링크로 옮기고, 모바일
  가로는 플레이 지원을 확장하지 않은 채 HUD 밀도만 보정한다.

## [2026-08-01] 구현 및 검증 | 홈·게임 전체 화면 레이아웃

- 홈의 고정 종횡비 구성판을 뷰포트 전체 좌표계로 바꾸고 책상·가마솥·
  슬라임을 화면 가장자리에 배치해 넓은 가로 화면의 빈 공간을 없앴다.
- 게임 프레임의 여백·테두리·8:5 크기 제한을 제거했다. 가로 화면은 맵을
  화면에 채우되 하단 작업자 정보를 보존하고, 세로로 긴 화면은 맵 비율을
  유지하면서 남는 영역을 동일한 목재 벽으로 채운다.
- 상시 홈 링크를 제거하고 음악 설정 모달 안에만 키보드 접근 가능한 홈
  이동을 추가했다. 낮은 가로 화면에서는 HUD와 선택 카드 밀도를 낮췄다.
- 검증 결과: **PASS**. 코어 테스트 22/22, lint, 프로덕션 빌드와
  `git diff --check`를 통과했다. 844×390, 900×1200, 1440×900에서 홈·
  선택·게임 화면과 설정 홈 링크의 숨김/표시를 확인했으며 콘솔 오류는 없다.

## [2026-08-02] 계획 | 불 슬라임·설비 타일·병합 검수

- 중복 슬라임 병합 뒤 `fire` 전용 분기가 `fire-1`에 적용되지 않고 원본 SVG
  로드도 빠진 회귀를 확인했다. 원본 몸통을 복구하고 방향별 얼굴과 깜빡임을
  Phaser에서 적용한다.
- 재료 상자·조리 도구·소각장은 한 타일 정사각형으로 보이게 하고, 보이는
  타일 전체를 우클릭 상호작용 영역으로 검증한다.
- Claude 병합분과 기존 그릇·전체화면 작업을 함께 돌렸을 때 코어 테스트
  26개 중 11개가 깨지는 의미 충돌, 추적된 TypeScript 생성물을 우선 고친다.

## [2026-08-02] 구현 및 검증 | 불 슬라임·설비 타일·병합 검수

- 불 슬라임 분기를 인스턴스 ID가 아닌 속성 타입으로 고쳐 원본 SVG를 다시
  불러왔다. 불꽃 여백을 고려해 몸통 폭을 다른 슬라임과 맞추고, 좌우 얼굴
  이동·위쪽 등 표시·정지와 이동 중 눈 깜박임을 같은 모션 컨테이너에 적용했다.
- 재료 상자·조리 도구·소각장을 56×56 정사각형으로 그리고 기존 60×60 타일
  전체를 우클릭 판정으로 유지했다. CSS 그림만 확대하던 좌표 오차를 캔버스
  자체 확대·크롭으로 바꿔 보이는 타일 모서리와 Phaser 입력을 일치시켰다.
- 중복 슬라임 선택 효과음의 인스턴스 ID 오류, 결과 화면의 가짜 타이틀 이동,
  그릇 순환과 충돌한 병합 테스트 11개를 정리하고 `tsconfig.tsbuildinfo`를
  추적 대상에서 제거했다. 추가본의 스테이지·정산 구현과 남은 소개·로딩·
  영입 범위를 `wiki/project.md`에 반영했다.
- 검증 결과: **PASS**. 코어 테스트 26/26, 변경 파일 타입 검사, lint,
  프로덕션 빌드, 셸 문법과 `git diff --check`를 통과했다. 브라우저에서
  불 슬라임 네 방향·실제 깜박임,
  세 설비 모서리 우클릭, GAME OVER 정산·타이틀 이동, 844×390·900×1200·
  1440×900 화면과 콘솔 오류 0건을 확인했다.

## [2026-08-03] 계획 | 스테이지 전환 정보 화면

- `stage_transition_info_cards_spec.md`와 `image(132).png`를 근거로 선택·정산과
  인게임 사이에 같은 정보 화면을 삽입하는 범위를 정했다.
- 기존 16×10 맵 데이터를 정적으로 렌더링하고, 음식 제작 흐름은 공용 레시피
  데이터에서 읽어 별도 Phaser 월드와 UI 전용 규칙 복제를 만들지 않는다.
- 현재 존재하지 않는 영입 화면과 실제 작업이 없는 로딩 화면은 구현하지 않고
  모든 기본 스테이지의 다음 단계를 인게임으로 연결한다.

## [2026-08-03] 구현 및 검증 | 스테이지 전환 정보 화면

- 첫 직원 선택 뒤와 다음 스테이지 정산 뒤에 같은 정보 화면을 연결했다. 제목,
  실제 16×10 주방 맵, TIP 2개, 3×2 음식 슬롯과 다음 버튼을 한 화면에 둔다.
- 버섯 구이 규칙을 코어의 공용 레시피로 옮겨 정보 화면이 버섯×1 → 조리
  도구·불 → 깨끗한 그릇 → 제출대를 실제 조리 규칙과 함께 읽게 했다.
- 정보 화면 동안 코어·Phaser·타이머를 멈추고 Q/W/E/R 입력을 무시한다.
  ESC 설정을 열고 닫아도 정보 화면을 보존하며 다음 중복 입력을 차단한다.
- 검증 결과: **PASS**. 코어 테스트 29/29, lint, 프로덕션 빌드를 통과했다.
  브라우저에서 1280×720과 844×390 레이아웃, 160개 맵 타일, 음식 6칸,
  타이머 정지, ESC 복귀, 음식 포커스 상세, 다음 더블클릭 1회 전환과 콘솔
  오류 0건을 확인했다.

## [2026-08-03] 계획 | 인게임 맵 UI·로컬 맵 에디터

- 사용자 참고 이미지 5장을 원문으로 보존하고 상단 주문 카드, 하단 선택키,
  우측 슬라임·설비 정보 패널과 도움말 오버레이를 구현 범위로 정했다.
- 기본 맵은 외곽 조리대와 중앙 작업 섬 구조로 바꾸되, 아직 코어 규칙이 없는
  동일 설비 여러 개와 도마 조리는 추가하지 않는다.
- 16×10 맵 데이터는 한 파일로 모으고, 배포 앱과 분리해 `127.0.0.1`에서만
  실행되는 에디터가 검증 후 직접 저장하도록 한다.

## [2026-08-03] 구현 및 검증 | 인게임 맵 UI·로컬 맵 에디터

- 외곽 조리대와 중앙 작업 섬이 있는 맵으로 바꾸고, 행·설비·상호작용 칸·
  스폰을 `game/map-data.ts` 하나에서 코어와 화면이 함께 읽게 했다.
- 상단 주문 카드 3장, 하단 속성 선택키, 우측 슬라임·설비 정보 패널과
  일시정지되는 상세 도움말을 연결했다. DOM 오버레이 클릭이 캔버스 아래
  오브젝트로 새지 않도록 Phaser 입력 경계에서 차단했다.
- `npm run map:edit`로만 여는 127.0.0.1 전용 에디터를 추가했다. Node 기본
  기능만 사용하며 고정 크기·설비 단일성·인접 작업 칸·스폰을 검사한 뒤
  맵 데이터 파일을 원자적으로 교체한다. 배포 앱에는 에디터 라우트가 없다.
- 검증 결과: **PASS**. 코어 테스트 30/30, 맵 검증, lint, 프로덕션 빌드,
  `git diff --check`와 1280×720·낮은 가로 화면 브라우저 검수를 통과했다.
  에디터 저장 200, 누락 설비 422, 비허용 Host 접근 403을 확인했다.

## [2026-08-03] 계획 | 맵 에디터 실행 스크립트·UI 에셋 목록

- 기존 `play-local.sh`와 같은 시작·종료 방식으로 로컬 맵 에디터를 여는
  루트 실행 스크립트 하나를 추가한다.
- 현재 코드 도형·이모지·임시 생성 SVG와 실제 `public/` 에셋을 대조해,
  게임 규칙을 늘리지 않고 교체가 필요한 UI·인게임 시각 에셋만 정리한다.

## [2026-08-03] 구현 및 검증 | 맵 에디터 실행 스크립트·UI 에셋 목록

- `edit-map.sh`가 에디터 서버를 시작하고 준비 완료 뒤 정확한 로컬 주소를
  열며, 셸이 끝날 때 서버도 함께 종료하도록 했다.
- 홈·물·불 에셋은 재사용하고, 1순위 설비 7종·아이템 4종·주방 타일·번개와
  땅 슬라임·화재 FX, 2순위 공용 UI 아이콘을 제작 목록으로 확정했다.
- 주문 카드 프레임·경고 띠·선택키·패널은 CSS를 유지해 중복 에셋 제작을
  제외했다.
- 검증 결과: **PASS**. 실행 권한, 셸 문법, 맵 데이터 검증, 프로덕션 빌드와
  `git diff --check`를 통과했으며 배포 라우트에 에디터가 포함되지 않았다.
