# NAN 2026 작업 규칙

이 저장소는 Phaser 3 웹 게임과 그 기획을 함께 관리한다.

## 우선순위

1. 사용자의 현재 요청
2. `wiki/project.md`의 확정된 기획과 완료 조건
3. 이 문서

충돌하거나 결정되지 않은 내용은 임의로 확장하지 말고 `wiki/project.md`의 열린 질문에 기록한다.

## 지식 구조

- `raw/`: 사용자가 제공한 원문. 읽기 전용이며 수정, 이동, 삭제하지 않는다.
- `wiki/`: 에이전트가 관리하는 현재 지식. 원문을 근거로 갱신한다.
- `wiki/index.md`: 모든 wiki 문서의 목록과 한 줄 설명.
- `wiki/log.md`: 작업 기록. 기존 항목은 수정하지 않고 새 항목만 추가한다.
- 게임 코드는 구현이 시작될 때 저장소 루트에 최소 구조로 추가한다.

Wiki 문서는 가능한 한 다음 frontmatter를 사용한다.

```yaml
---
type: project | source | plan | decision
status: draft | current | superseded
updated: YYYY-MM-DD
sources:
  - raw/...
---
```

근거 없는 사실을 확정된 기획처럼 쓰지 않는다. 원문과 해석을 구분하고, 모순이나 미결정 사항은 명시한다.

## 기능 개발 흐름

각 기능은 `계획 → 작업 → 검증` 순서로 진행한다. 역할은 서로의 범위를 침범하지 않는다.

### 계획

- 먼저 `wiki/index.md`와 관련 문서를 읽는다.
- 구현 범위, 구현하지 않을 범위, 검증 가능한 완료 조건을 작성한다.
- 코드를 수정하지 않는다.

### 작업

- 승인되거나 합의된 계획의 범위만 구현한다.
- Ponytail 원칙에 따라 기존 코드, 브라우저 기능, Phaser 기능, 이미 설치된 의존성을 우선한다.
- 완료 조건과 무관한 추상화, 설정, 의존성, 미래용 구조를 추가하지 않는다.
- 비자명한 로직에는 가장 작은 실행 가능한 검증을 하나 남긴다.

### 검증

- 완료 조건을 기준으로 빌드와 실제 동작을 확인한다.
- 기능을 확장하거나 새로운 설계를 추가하지 않는다.
- 결과를 `PASS`, `FIX`, `REPLAN` 중 하나로 판정한다.
- 같은 원인이 반복되면 임의로 반복 수정하지 말고 사용자 판단을 요청한다.

## 게임 구현 제약

- Gemini는 음성 의도를 허용된 명령 JSON으로 구조화할 뿐, 행동 결과를 결정하지 않는다.
- actor, action, target은 현재 게임 상태의 허용 목록으로 검증한다.
- 게임 로직은 Phaser 렌더링 없이 실행 가능해야 한다.
- 같은 입력과 랜덤 시드는 같은 결과를 만들어야 한다.
- 신뢰 경계의 입력 검증, 데이터 손실 방지, 보안, 접근성은 단순화를 이유로 제거하지 않는다.

## Wiki 갱신

원문 추가 또는 중요한 결정이 있을 때:

1. 관련 wiki 문서를 갱신한다.
2. `wiki/index.md`의 설명이나 상태가 달라졌으면 함께 갱신한다.
3. `wiki/log.md`에 `## [YYYY-MM-DD] 종류 | 제목` 형식으로 추가한다.

